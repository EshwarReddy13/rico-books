export type EntitySummary = {
  id: string;
  name: string;
  description: string;
  colorHex: string;
};

export type EntityFormValues = {
  name: string;
  description: string;
  colorHex: string;
};
